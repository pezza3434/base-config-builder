{
  "name": "base-config-builder",
  "version": "0.0.1",
  "description": "Builds base page configs for the Laterooms SWP",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "bin": {
    "base-config-builder": "index.js"
  }
}
